# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties
.flattened-pom.xml
dependency-reduced-pom.xml

# IDE
.idea/
*.iml
.project
.classpath
.settings/
.factorypath
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# Logs
*.log
logs/

# JVM / diagnostics (crash dumps, heap dumps)
hs_err_pid*
replay_pid*
*.hprof

# Allure (local CLI config; results also land under target/ when using Maven)
.allure/
**/allure-results/
**/allure-report/

# Playwright (Java)
playwright/.cache/

# TestNG default output if tests are run outside Maven
test-output/

# Test / local env
.env
.env.*
!.env.example
*.local.properties

# Pact broker / ad-hoc pact drops (generated pacts live under target/, already ignored)
pacts/

# Editor / tooling (local only; remove the next line if you commit shared Cursor rules)
.cursor/
